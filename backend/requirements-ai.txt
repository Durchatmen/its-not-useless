# 目标环境：Python 3.14.7 / Windows x64
# 本文件需在 requirements.txt 之后安装，两者共用同一虚拟环境。
# 状态：已通过依赖解析验证（pip install --dry-run --report，含 requirements.txt，
#       共 217 个包，退出码 0，pydantic 保持 2.13.5 不被降级）。
#       ⚠ 尚未做真机安装验证，拉取体积很大（含 torch 2.14.0）。

# --- Agent 编排 ---
langgraph==1.2.11
langchain==1.4.1
langchain-community==0.4.2

# --- 向量库与检索 ---
pymilvus==3.0.2
rank-bm25==0.2.2

# --- Embedding 与重排（BGE-M3 / BGE-reranker-v2-m3）---
FlagEmbedding==1.4.2

# --- 文档解析与 OCR ---
# MinerU 项目现用包名（旧名 magic-pdf；1.3.11+ 的 requires_python 已排除 3.14）。
mineru==4.0.2
paddleocr==3.7.0

# --- 观测（本地可选）---
langfuse==4.15.4

# ⚠ 已知安装阻塞：paddlex（paddleocr 的依赖，最新 3.7.2 亦然）硬性要求 PyYAML==6.0.2，
#   而 PyYAML 6.0.2 无 cp314 wheel，Windows 上会退回源码包、需要 MSVC 编译。
#   PyYAML 6.0.3 有 cp314 wheel 但被 == 精确锁排除。
#   处理方式见项目文档「环境搭建方案」待办事项。
